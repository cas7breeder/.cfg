1. `git clone --bare git@github.com:cas7breeder/.cfg.git $HOME/work/.cfg`
2. `config update-index --skip-worktree ~/README.md && rm ~/README.md`
3. `config config --local status.showUntrackedFiles no`
