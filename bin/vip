#!/bin/bash

TMPFILE=`mktemp /tmp/vip_tmp_file.XXXXXXXX`

# cat stdin (default)
cat > ${TMPFILE}

# Open with the default editor
${VISUAL} ${TMPFILE} < /dev/tty > /dev/tty

cat ${TMPFILE}

rm ${TMPFILE}
